# 公开范围与发布审计

更新日期：2026-09-17。仓库现包含当前开发截图、架构与工程摘要；下面的 Release 包审计仅对应 2026-09-04 的 `v0.4.1-preview`，本次未更换安装包。

## 仓库包含

- 产品说明、架构、状态、路线图和安全边界；
- 11 张带环境标识的当前开发界面隔离截图，以及保留原版本的 8 张历史预览截图；
- AI 协作说明与脱离账户信息的工程案例摘要；
- 0.0.1 历史演示说明；
- 0.4.1 Release 说明与公开导出清单。

本仓库不公开私有主项目源码。

## Release 包包含

- Electron 桌面运行时；
- 当前 Web UI 静态资源；
- 专用的只读 Showcase Product API；
- 图片与启动失败页；
- `showcase-mode.json` 运行标记。

## Release 包不包含

- 私有研究任务脚本及其实现；
- 生产启动器、生产 Canary、授权签发器或凭据配置；
- 真实签名、广播、下单、卖出或清仓实现；
- 钱包、私钥、Token、真实地址和生产配置；
- 源码映射、Harness、私有历史与内部 Evidence；
- 主项目 Git 历史或完整源码。

## 构建隔离方式

公开包不是从完整桌面包中“删几个文件”得到，而是使用独立 Showcase 构建：

1. 编译时启用公开预览常量；
2. 完整 Product API 替换为专用只读 API；
3. 市场服务替换为不发起远程请求的公开实现；
4. 私有任务按钮运行时锁定；
5. 打包白名单只允许公开入口、静态 Web 资源和必要图标；
6. 构建后解包扫描实际产物。

## 2026-09-04 发布门槛

| 门槛 | 结果 |
| --- | --- |
| 当前 UI 截图人工检查 | 通过 |
| Lint / TypeScript | 通过 |
| 测试 | 2049 通过、5 跳过 |
| 服务端 / Web / 桌面构建 | 通过 |
| Windows 安装版与便携版 | 已生成 |
| 最终包烟雾测试 | 退出码 0 |
| ASAR 内容审计 | 通过 |
| 硬编码秘密与私人路径扫描 | 未检出 |
| 本机 Microsoft Defender | 两个 EXE 未发现威胁 |
| SHA-256 | 已随 Release 提供 |
| Authenticode 代码签名 | 未完成 |
| 开源许可证 | 未决定 |
| 独立保密安全联系渠道 | 未提供 |

未完成项必须保留为限制，不能在宣传中省略。

## 本次文档和图片更新

本次没有公开主项目源代码、编译后的私有 Web 资源、生产数据或个人简历。截图使用空会话测试桥接，不包含真实钱包与交易。历史 `public-export-manifest.json` 继续只描述 9 月 4 日的包；本次图集另见 [截图清单](docs/screenshot-manifest-2026-09-17.json)。

当前没有开源许可证。公开文档、图片与评估包不自动授予修改或再分发许可。
