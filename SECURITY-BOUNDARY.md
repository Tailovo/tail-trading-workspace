# TAIL 安全边界

## 当前定位

TAIL 0.4.1 Showcase 是只读、离线优先的产品预览，不是自动实盘交易产品。

## 发布包物理排除

- 私钥、助记词、钱包文件、Token、Cookie 和生产凭据；
- 私有研究任务脚本；
- 生产启动器、生产 Canary 和授权签发工具；
- 真实签名、广播、下单和退出实现；
- 私有治理、事故、冻结与外部 Evidence；
- 源码映射以及可暴露本机路径的调试产物。

## 运行时限制

- Product API 仅绑定回环地址；
- 公开模式锁定私有任务入口；
- 默认不连接钱包、账户或真实行情；
- 合成与 Fixture 数据必须在界面上明确标注；
- 未知、缺失、断连和未授权状态不能被伪装成可用；
- 内置浏览器只允许 HTTPS，并阻断本地、私网和带凭据地址。

## 本次候选检查

最终 ASAR 解包包含 9 个应用文件。以下检查均为 0：

- 源码映射；
- 研究任务脚本文件；
- Harness 文件；
- 生产启动器与生产 Canary 标识；
- 已知研究钱包地址；
- 私人绝对路径；
- PEM 私钥块与疑似硬编码凭据。

两个发布 EXE 在构建机当前 Microsoft Defender 定义下均显示“未发现威胁”。这不是第三方安全认证，也不能代替用户自己的下载校验和终端防护。

## 签名状态

当前 EXE 未进行 Authenticode 代码签名，因此 Windows 可能显示未知发布者或 SmartScreen 提示。请从本仓库 Release 下载并核对 SHA-256。

## 能力声明

界面按钮、源码存在、历史测试或本地状态都不能单独证明真实资金能力。真实能力至少需要当前实现、显式授权、可用依赖、运行证据和安全门槛同时成立。

## 漏洞与敏感信息

普通缺陷可提交 GitHub Issue。不要在公开 Issue 中提交私钥、Token、账户、真实余额、交易记录或可利用的敏感细节。当前尚未公布独立的保密安全联系渠道；如 GitHub Security 页面提供“Report a vulnerability”，优先使用该入口，否则只提交不含敏感信息的最小问题说明。
