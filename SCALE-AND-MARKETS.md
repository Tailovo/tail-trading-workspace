# 规模与市场边界

## “数千级”不是一个完整指标

在公开容量声明前，必须说明测量对象：

| 维度 | 需要定义 |
| --- | --- |
| 信号源 | 数量、更新频率和突发量 |
| 跟单关系 | 单源关系数和总关系数 |
| 账户 | 在线、持仓和待核对数量 |
| 策略 | 并行实例和覆盖标的 |
| 市场事件 | 平均与峰值每秒处理量 |
| 订单意图 | 平均、峰值和限速 |
| 持仓 | 活动数量和核对周期 |

当前只能说“目标验证数千级关系与多策略并行”，不能说“已经支持数千账户实盘”。

## 容量验证必须公开

- 测试环境、数据来源、负载模型和持续时间；
- 端到端延迟的 P50、P95 和 P99；
- 事件吞吐、积压量和恢复时间；
- 重复、遗漏和乱序事件数量；
- 意图到最终核对的收敛时间；
- 断线、重启和依赖不可用后的恢复表现；
- 单账户、单策略与系统级故障是否隔离。

## 多市场原则

可以共用：

- 策略和跟单生命周期；
- 信号、风险、订单意图和执行回报的外层语义；
- Evidence、回放、诊断与人工接管；
- 能力、授权和状态语言。

必须隔离：

- 行情协议和数据授权；
- 交易日历、时段和停牌/暂停状态；
- 价格、数量、订单类型和费用规则；
- 执行协议、成交确认、撤单和恢复方式；
- 法律、平台和账户授权边界。

TAIL 当前不把任何尚未独立验证的市场写成已接入。Solana 是主要研究参考场景；其他环境仍属于未来评估。
