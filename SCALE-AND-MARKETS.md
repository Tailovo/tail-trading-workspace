# 规模目标与多市场边界

## “数千级”需要先定义

“支持几千”不是可测试的指标。公开容量声明前，必须明确至少以下维度：

| 维度 | 需要定义的内容 |
| --- | --- |
| 信号源 | 同时跟踪多少来源，更新频率和突发量是多少 |
| 跟单关系 | 一个信号源对应多少账户，总关系数是多少 |
| 账户 | 同时在线、持仓和待核对账户数量 |
| 策略 | 同时运行的策略实例及每个实例覆盖的标的数量 |
| 市场事件 | 平均与峰值每秒处理量 |
| 订单意图 | 平均、峰值及单账户限速 |
| 持仓 | 同时活动持仓数量和核对周期 |

当前推荐的产品表达是“目标支持数千级跟单关系与多策略并行”，不是“已经支持数千账户实盘”。

## 必须公开的验证指标

- 端到端延迟的中位数、95 分位和 99 分位；
- 市场事件处理吞吐与积压恢复时间；
- 重复订单意图率与重复成交影响；
- 漏处理事件数量；
- 订单意图到最终核对的收敛时间；
- 断线、进程重启和依赖不可用后的恢复时间；
- 单账户、单策略和系统级停止是否相互隔离；
- 测试环境、数据来源、负载模型和测试持续时间。

## 规模设计原则

1. 使用可重放事件和幂等订单意图，不假设所有消息恰好到达一次。
2. 通过分区保证同一账户或同一策略要求的局部顺序。
3. 用背压和限速保护执行适配器，不让行情突发直接冲击账户。
4. 权威状态来自执行回报和周期核对，不来自界面或单次请求结果。
5. 每个账户和策略都有独立风险预算、停止状态和恢复状态。
6. 桌面控制台可以退出，持续运行服务不能依赖桌面窗口存活。

## 多市场共用与隔离

### 可以共用

- 策略和跟单生命周期；
- 信号、订单意图和执行回报的统一外层模型；
- 账户、组合与风险预算；
- 事件、证据、回放、诊断和人工接管；
- 能力、授权和状态语言。

### 必须按市场隔离

- 数据源和行情协议；
- 交易日历、时段和停牌状态；
- 价格、数量、订单类型和费用规则；
- 券商、交易平台或链上执行协议；
- 成交确认、撤单、失败与恢复方式；
- 市场与数据使用约束。

## A股扩展原则

A股适配器应把交易日历、交易时段、数量与价格约束、停牌状态、券商回报和组合风控转换成统一控制层可以理解的能力与状态。策略可以复用研究和风控框架，但不能假设链上市场的连续交易、确认方式或流动性模型仍然成立。

接入具体券商或数据源前，需要单独核验接口可用性、数据授权、自动化边界、成本和运行责任；本文件不把未来适配方向写成当前接入事实。
